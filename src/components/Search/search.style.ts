import {StyleSheet} from 'react-native';

export const searchStyle = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: 'gray',
    paddingHorizontal: 20,
    borderRadius: 8,
  },
});
